/**
 * Placeholder — implement with iron-session or jose.
 * See docs/ARCHITECTURE.md §4 Authentication Approach
 */
export type SessionPlayer = {
  playerId: string;
  displayName: string;
  isAdmin: boolean;
};
